<!-- GENERATED by `supervisor/src/stubs.rs` from the Rust types and `intro/system.md`. Do not edit: run `just stubs`. -->

# system

Wall and monotonic clocks, pushed once a second.

```lua
text {
    content = mantle.system:map(function(system)
        return system and os.date("%a %H:%M", system.time) or ""
    end),
}
```

## State

`mantle.system:get()` returns `SystemState`, `nil` before the first push. A field marked `?` may be absent.

`mantle.system`'s payload, pushed once a second.

| Field | Type | Description |
| --- | --- | --- |
| `monotonic` | `integer` | Seconds since `system` was first used; excludes suspend. Take durations from it, since NTP moves `time`. |
| `time` | `integer` | Unix epoch seconds, as `os.date` takes them. |

## Actions

None: read-only, so an `invoke` is logged and dropped.

## Backend

Ticks once a second, aligned to the wall-clock second at start only; an NTP step or resume is not
re-aligned. `time` is epoch seconds; `monotonic` counts seconds from the capability's start and
excludes suspend.

## How do I…

| Task | Answer |
| :--- | :--- |
| Show a clock | `os.date` over `mantle.system.time`, as in the example above |

See also: [Clock bar](../cookbook/clock-bar.md) recipe.

Source: [`supervisor/src/capabilities/system/`](../../supervisor/src/capabilities/system/)
